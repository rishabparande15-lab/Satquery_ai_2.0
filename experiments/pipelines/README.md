# Experimental Pipelines

This directory will contain three experimental approaches:

1. Existing local BigEarthNet + CROMA pipeline
2. GEE-only pipeline
3. GEE + CROMA pipeline

The final comparison will be performed only after suitable alternative datasets and evaluation methods are available.
