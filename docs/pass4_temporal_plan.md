# Pass 4 Temporal Plan

Status: **NOT SUPPORTED BY THESE ARCHIVES**.

Acquisition dates appear in Sentinel-1 and Sentinel-2 product identifiers, but each selected area has one S1 acquisition identity and one corresponding S2 identity in metadata. No before/after pair IDs, change labels, change questions, or change maps were present. The official archive has no usable metadata or reconstructible complete streams.

These files therefore cannot support a temporal/change benchmark. An arbitrary pair of acquisition dates must not be interpreted as a temporal training pair. Temporal prediction remains unresolved.